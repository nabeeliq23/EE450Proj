#include <iostream>
#include <fstream>
#include <sstream>
#include <string>
#include <vector>
#include <map>

std::map<std::string, std::vector<std::vector<int>>> readInput(std::string fileName){
    std::ifstream inputFile(fileName); // Replace "data.txt" with your input file name
           
    std::string line;
    std::map<std::string, std::vector<std::vector<int>>> dataMap;      

    return dataMap; 
}

bool classOverlap(std::vector<std::vector<int>> stud1, std::vector<std::vector<int>> stud2){
    return false; 
}